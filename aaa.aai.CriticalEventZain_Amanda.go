package main

/* Component is yet to be developed. Its file only exist at the moment so as to reserve the ID "aaa.aai", for it. */

func iReport_CriticalEventZain_Amanda (eventDescription string) {
	fmt.Println ("        CRITICAL EVENT:" + eventDescription)
}
